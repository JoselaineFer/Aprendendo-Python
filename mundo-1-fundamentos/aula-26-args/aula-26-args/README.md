# Aula 26 - *args

   Aprendi que uma função pode receber uma quantidade variável de valores.

   - `*numeros` recebe qualquer quantidade de argumentos, guardados como uma lista.
   - A mesma função funciona não importa quantos valores eu passe na chamada.

   ## Como rodar
   No terminal, dentro desta pasta: `python main.py`
