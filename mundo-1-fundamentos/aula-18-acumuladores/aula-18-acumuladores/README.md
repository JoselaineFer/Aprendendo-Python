# Aula 18 - Acumuladores

   Aprendi que um acumulador guarda a soma de todos os valores passados durante o loop.

   - Começo o "cofrinho" em zero, antes do loop.
   - A cada volta, atualizo o valor: `soma = soma + numero`.

   ## Como rodar
   No terminal, dentro desta pasta: `python main.py`
