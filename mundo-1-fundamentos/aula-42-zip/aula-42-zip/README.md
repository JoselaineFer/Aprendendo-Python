# Aula 42 - zip()

   Aprendi a juntar duas listas, posição por posição.

   - `zip(lista1, lista2)` combina os itens de mesma posição das duas listas.
   - Para automaticamente quando a lista menor acaba.

   ## Como rodar
   No terminal, dentro desta pasta: `python main.py`
